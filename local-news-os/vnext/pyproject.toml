[project]
name = "local-news-os-vnext"
version = "2.0.0"
requires-python = ">=3.12"
dependencies = [
  "psycopg[binary]>=3.2,<4",
  "starlette>=0.47,<1",
]

[tool.vercel]
entrypoint = "vercel_app:app"
