name: VÂLCEA CLAR CJRAE Education Support Signals

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'valcea-clar/scripts/cjrae_valcea_education_support_signal_adapter.py'
      - '.github/workflows/valcea-clar-cjrae-education-support-signals.yml'
  push:
    branches: [main]
    paths:
      - 'valcea-clar/scripts/cjrae_valcea_education_support_signal_adapter.py'
      - '.github/workflows/valcea-clar-cjrae-education-support-signals.yml'

permissions:
  contents: read

concurrency:
  group: valcea-clar-cjrae-education-support-signals-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - name: Compile CJRAE Vâlcea education-support adapter
        run: python -m py_compile valcea-clar/scripts/cjrae_valcea_education_support_signal_adapter.py
      - name: Run CJRAE Vâlcea fail-closed regressions
        run: python valcea-clar/scripts/cjrae_valcea_education_support_signal_adapter.py --self-test
