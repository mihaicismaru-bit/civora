name: EUCONS Provenance Freshness

on:
  pull_request:
    paths:
      - 'eucons/prospects/client_finder_provenance_freshness.py'
      - 'eucons/prospects/client_finder_provenance_freshness_contract.json'
      - 'eucons/validation/test_client_finder_provenance_freshness_fail_closed.py'
      - '.github/workflows/eucons-provenance-freshness.yml'
  push:
    branches: [main]
    paths:
      - 'eucons/prospects/client_finder_provenance_freshness.py'
      - 'eucons/prospects/client_finder_provenance_freshness_contract.json'
      - 'eucons/validation/test_client_finder_provenance_freshness_fail_closed.py'
      - '.github/workflows/eucons-provenance-freshness.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: eucons-provenance-freshness-${{ github.ref }}
  cancel-in-progress: true

jobs:
  fail-closed:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - name: Test Client Finder provenance freshness fail-closed behavior
        run: python eucons/validation/test_client_finder_provenance_freshness_fail_closed.py
