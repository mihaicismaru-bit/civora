name: VÂLCEA CLAR DSP Health Signals

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'valcea-clar/scripts/dsp_valcea_health_signal_adapter.py'
      - '.github/workflows/valcea-clar-dsp-health-signals.yml'
  push:
    branches: [main]
    paths:
      - 'valcea-clar/scripts/dsp_valcea_health_signal_adapter.py'
      - '.github/workflows/valcea-clar-dsp-health-signals.yml'

permissions:
  contents: read

concurrency:
  group: valcea-clar-dsp-health-signals-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - name: Compile DSP health signal adapter
        run: python -m py_compile valcea-clar/scripts/dsp_valcea_health_signal_adapter.py
      - name: Run DSP fail-closed regressions
        run: python valcea-clar/scripts/dsp_valcea_health_signal_adapter.py --self-test
