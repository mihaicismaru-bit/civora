name: VÂLCEA CLAR ETA Signals

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'valcea-clar/scripts/eta_valcea_signal_adapter.py'
      - '.github/workflows/valcea-clar-eta-signals.yml'
  push:
    branches: [main]
    paths:
      - 'valcea-clar/scripts/eta_valcea_signal_adapter.py'
      - '.github/workflows/valcea-clar-eta-signals.yml'

permissions:
  contents: read

concurrency:
  group: valcea-clar-eta-signals-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - name: Compile ETA signal adapter
        run: python -m py_compile valcea-clar/scripts/eta_valcea_signal_adapter.py
      - name: Run ETA fail-closed regressions
        run: python valcea-clar/scripts/eta_valcea_signal_adapter.py --self-test
