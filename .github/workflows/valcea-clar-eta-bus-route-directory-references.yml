name: VÂLCEA CLAR ETA Bus Route Directory References

on:
  pull_request:
    paths:
      - "valcea-clar/scripts/eta_bus_route_directory_reference_adapter.py"
      - ".github/workflows/valcea-clar-eta-bus-route-directory-references.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: valcea-clar-eta-bus-route-directory-references-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Compile bounded adapter
        run: python -m py_compile valcea-clar/scripts/eta_bus_route_directory_reference_adapter.py
      - name: Run fail-closed self-test
        run: python valcea-clar/scripts/eta_bus_route_directory_reference_adapter.py --self-test
      - name: Verify live first-party source health
        shell: bash
        run: |
          python valcea-clar/scripts/eta_bus_route_directory_reference_adapter.py > /tmp/eta-bus-routes.json
          python - <<'PY'
          import json
          with open("/tmp/eta-bus-routes.json", "r", encoding="utf-8") as handle:
              data = json.load(handle)
          assert data["state"] == "CLEAR", data
          assert data["reference_count"] >= 8, data
          assert data["content_contract"] == "FIRST_PARTY_TRANSIT_ROUTE_DIRECTORY_REFERENCE_ONLY"
          assert all(item["schedule_currentness_authorized"] is False for item in data["references"])
          assert all(item["current_service_state_authorized"] is False for item in data["references"])
          assert all(item["realtime_authorized"] is False for item in data["references"])
          print(f"live-source: clear ({data['reference_count']} route references)")
          PY
