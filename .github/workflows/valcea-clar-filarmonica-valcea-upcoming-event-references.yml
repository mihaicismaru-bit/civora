name: VÂLCEA CLAR Filarmonica Vâlcea Upcoming Event References

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'valcea-clar/scripts/filarmonica_valcea_upcoming_event_reference_adapter.py'
      - '.github/workflows/valcea-clar-filarmonica-valcea-upcoming-event-references.yml'
  push:
    branches: [main]
    paths:
      - 'valcea-clar/scripts/filarmonica_valcea_upcoming_event_reference_adapter.py'
      - '.github/workflows/valcea-clar-filarmonica-valcea-upcoming-event-references.yml'

permissions:
  contents: read

concurrency:
  group: valcea-clar-filarmonica-valcea-upcoming-event-references-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - name: Compile Filarmonica Vâlcea event reference adapter
        run: python -m py_compile valcea-clar/scripts/filarmonica_valcea_upcoming_event_reference_adapter.py
      - name: Run Filarmonica Vâlcea fail-closed regressions
        run: python valcea-clar/scripts/filarmonica_valcea_upcoming_event_reference_adapter.py --self-test
