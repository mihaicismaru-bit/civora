# Canonical import provenance

This document records the repository bootstrap only. It is not the current CIVORA implementation status.

The GitHub repository was initialized from the validated local artifact `CIVORA_core_runtime_v0_2.zip` on 2026-08-06. Local validation before that initial publication was 4/4 unit tests passed.

After bootstrap, GitHub became the canonical persistence/version-control layer. Subsequent checkpoint implementations were committed directly to the canonical development branch together with checkpoint documentation, evidence ledgers, tests and GitHub Actions validation.

For current status, use the latest checkpoint/evidence ledger and the active pull-request head rather than this historical import record.
