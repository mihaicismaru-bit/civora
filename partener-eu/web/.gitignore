# Transient deployment trigger markers are runtime/debug artifacts, not product state.
# Durable evidence belongs under PARTENER.EU validation/checkpoint paths, not web/.
.deploy-batch-*
.mipe-delivery-*
.mipe-deploy-refresh-*
.mipe-live-refresh
